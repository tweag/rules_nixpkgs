[project]
name = "import-packages-test"
version = "0.1.0"
description = "Test of python packages imports in Bazel"
requires-python = ">=3.13"
dependencies = [
    "flask>=3.0.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.uv]
package = false
